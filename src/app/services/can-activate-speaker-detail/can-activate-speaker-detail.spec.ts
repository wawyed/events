import { CanActivateSpeakerDetail } from './can-activate-speaker-detail';

describe('CanActivateSpeakerDetail', () => {
  it('should create an instance', () => {
    expect(new CanActivateSpeakerDetail()).toBeTruthy();
  });
});
